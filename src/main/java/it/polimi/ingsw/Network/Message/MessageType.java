package it.polimi.ingsw.Network.Message;

//Enumerations that indicates in what state of the connection the message is sent
public enum MessageType {
    FIRST_CONNECTION, CONNECTION_RESPONSE, YOUR_TURN,
}
